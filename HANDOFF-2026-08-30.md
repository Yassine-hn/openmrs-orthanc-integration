# HANDOFF — remaining work, OHIF/Orthanc integration

**Date:** 2026-08-30
**Project:** openmrs-orthanc-integration — Neurosurgery, CHU Blida
**Server:** `server-PowerEdge-T140`, LAN `10.0.211.249` (`eno1`)
**Imaging module:** **1.2.0**, deployed and running

---

## 0. Read this first

The OHIF integration **works**. This document lists only what is left.

Before doing anything here, read:

| Document | For |
| --- | --- |
| `CLAUDE.md` → *Project-specific rules* | working protocol and the traps that have actually bitten |
| `OHIF-Integration-Architecture.md` | the OHIF chain: TLS, routing, auth injection |
| `README.md` | the deployed system, configuration, troubleshooting |

**Carry over the working protocol:** one change at a time; read-only before any write;
timestamped backups into `backup files/`; state WHERE each command runs; never claim
something works without demonstrating it. **No `git` write commands without explicit
authorisation** (this was granted, and used, on 2026-09-16/17 — see §5).

> **⚠️ Read §4.3 before this project ever moves toward production.** The GitHub repo
> backing this project is **public** and its current tip carries live, working
> credentials. This is a recorded, deliberate decision for the development stage — not an
> oversight — but it is a hard gate before anything resembling production use.

---

## 1. Verified working — do not redo

| Thing | Evidence |
| --- | --- |
| HTTPS chain end to end | `https://viewer.hospital.lan/` → 200; `/dicom-web/studies?limit=1` → 200 `application/dicom+json` |
| **No password prompt** | no `WWW-Authenticate` header; auth injected server-side by `orthanc-cors-proxy` |
| OHIF loads studies | NPM host-3 access log: 23 metadata + 17 frame requests, **all 200**, 7.1 MB delivered |
| Module 1.2.0 deployed | `imaging-1.2.0.omod` installed and started |
| **Stale-study pruning (Bug 1)** | rows 6 → 2, matching Orthanc exactly, plus `WARN … Removing study 1.2.826.0.1.3680043.8.853.2.1245427 assigned to patient 9: no longer present on http://orthanc:8042` |
| Image rendering | confirmed working from other hosts. This server has no GPU and is **not** a display machine — that is expected and is not a defect. |

Two log lines that look alarming and are **not**:

- `ERROR - ImagingActivator.started(29) Started Imaging` — a success message at the wrong level.
- `IllegalStateException: Filters can not be added to context /openmrs …` — normal after a hot module upload.

---

## 2. Priority 1 — quick wins

### 2.1 `client_max_body_size` — ✅ DONE 2026-09-01

**Applied.** `client_max_body_size 0;` added at the `server` level of
`orthanc-cors-proxy.conf`, validated with `nginx -t`, applied with a graceful
`nginx -s reload`. Inode preserved (bind mount intact).

**Verified** with a POST to a nonexistent path, which creates nothing:

| Probe | Before | After |
| --- | --- | --- |
| 2 MB POST via `orthanc-cors-proxy:8043` | 413 | **404** (forwarded to Orthanc) |
| 2 MB POST via `https://viewer.hospital.lan` | 413 | **404** |
| 20 MB POST via `https://viewer.hospital.lan` | — | **404** |
| `GET /dicom-web/studies?limit=1` (regression) | 200 | **200** `application/dicom+json` |

**Correction to an earlier assumption:** Nginx Proxy Manager was *not* a second ceiling.
It sets `client_max_body_size 2000m;` in its own `/etc/nginx/nginx.conf`. The 413 initially
observed through NPM was the CORS proxy's 413 being relayed back through it. **No NPM change
was needed.**

**Still to confirm by actually using it:** save a segmentation brush stroke from a
workstation. The transport limit is proven lifted; whether OHIF's STOW-RS write succeeds
end to end is a separate assertion that only a real save can settle.

**ROLLBACK:** `cat "backup files/orthanc-cors-proxy.conf.bak-20260901-104751" > orthanc-cors-proxy.conf`
then `docker exec orthanc-cors-proxy nginx -t && docker exec orthanc-cors-proxy nginx -s reload`

---

### 2.2 Install the hospital CA — ✅ DONE 2026-09-07

**Applied.** `hospitalCA.crt` copied to `/usr/local/share/ca-certificates/` and
`update-ca-certificates` run: "1 added, 0 removed; done."

**Verified:**

| Check | Before | After |
| --- | --- | --- |
| `curl` (no `-k`) `https://viewer.hospital.lan/` | `000` (refused) | **`200`** |
| `curl` (no `-k`) `https://orthanc.hospital.lan/` | — | **`401`** (Orthanc's own auth challenge — TLS itself now succeeds) |
| `curl` (no `-k`) `https://openmrs.hospital.lan/` | — | **`302`** (OpenMRS login redirect — same) |
| `openssl s_client -CApath /etc/ssl/certs` full chain | — | **`Verify return code: 0 (ok)`**, both `CHU Blida Internal CA` and the leaf cert |

This applies to the **system trust store** on this host — any tool that reads it
(`curl`, `wget`, Java's `cacerts`) now trusts the chain. It does **not** cover browsers:
Chrome and Firefox keep their own separate stores, so a warning there is expected and is
not a sign this step failed. If it persists, use the `certutil` import documented
originally in this section (requires `libnss3-tools`, not installed on this host).

**Still needed:** the CA must be installed the same way on every **clinical
workstation** — this only covers the server.

**ROLLBACK:** `sudo rm /usr/local/share/ca-certificates/hospitalCA.crt && sudo update-ca-certificates --fresh`

---

### 2.3 Confirm the OHIF icon on the "Get studies" page (Bug 2) — ✅ DONE 2026-09-07

**Confirmed visually** and backed by independent server-side evidence — this was tested
via `studies.page` → **Get Studies** button → `syncStudies.page`, exactly the path
Bug 2 affected.

`~/nginx-proxy-manager/data/logs/proxy-host-3_access.log` shows a real study load
through the icon, minutes before this was reported:

```
[07/Sep/2026:10:24:55] GET .../frames/1  ->  200, 556548 bytes
  Referer: https://viewer.hospital.lan/viewer?StudyInstanceUIDs=1.2.826.0.1.3680043.8.853.2.1116259
  User-Agent: Firefox/154.0
```

Five consecutive frame requests, all 200, via `/viewer` (not `/segmentation`) — the OHIF
link built by `syncStudies.gsp` opened correctly and the study rendered.

**This closes Priority 1 in full.** All of §2.1, §2.2, §2.3 are done.

---

### 2.4 Error page crash on "green" theme — ⚠️ PARTIALLY FIXED 2026-09-07, root cause not captured

**What was seen:** `http://10.0.211.249:8080/openmrs/` returned a real HTTP 500 in a browser
session (French→English language switch, done earlier the same day). The page shown was
not OpenMRS's own error page but Tomcat's generic fallback, reporting:

```
javax.servlet.jsp.JspTagException: Theme 'green': No message found under code 'favicon' for locale 'en_US'.
```

**Root cause of the crash itself (confirmed, not guessed):** OpenMRS's own error page
(`WEB-INF/view/uncaughtException.jsp`) tries to render a `<spring:theme code="favicon"/>`
tag using whatever theme is active. The `chublidatheme` module forces theme `green`
(`ThemeInjectionFilter`) for every request. `green`'s resource bundle — bundled inside the
stock `legacyui-1.8.4.omod`, **not custom to this project** — only defines 3 keys
(`image.logo.bar`, `stylesheet`, `jqueryui.theme.name`) and does not inherit `favicon` from
`defaults.properties` the way OpenMRS's own themes do. So whenever *any* unrelated
exception occurs, the page built to display it throws a second exception, and the real
error is never shown to the browser and — separately — was not in any log file either,
because the JVM's `MEMORY_APPENDER` (the Log4j appender OpenMRS uses to capture error
detail) is not started: `ERROR Attempted to append to non-started appender MEMORY_APPENDER`
recurs in `docker logs openmrs-app` on unrelated scheduled tasks too, so this is a
standing, pre-existing logging gap, not something introduced today.

**Applied:** added the missing keys to the live resource file, matching the values
`defaults.properties` already uses:

```
favicon=/images/openmrs-favicon.ico
favicon.png=/images/openmrs-favicon.png
```

**WHERE:** inside the `openmrs-app` container, in the **persistent** `openmrs_data` named
volume (survives restarts) — NOT in this git-less project's own files:
`/usr/local/tomcat/.OpenMRS/.openmrs-lib-cache/reportingcompatibility/themes/green.properties`

Backed up first to `green.properties.bak-20260907-152840` in the same directory (inside the
container/volume — not in `backup files/`, since that convention covers host files and this
lives only in the Docker volume).

**Applied via:** `docker restart openmrs-app` at 15:30:32, back up and responding within
~2 minutes (same order of downtime as the c3p0 fix in §4.1).

**Verified:**

| Check | Result |
| --- | --- |
| All 7 project containers + NPM | `Up`, MySQL/Postgres/medreport-rgs `healthy` |
| `http://10.0.211.249:8080/openmrs/` | 200, renders the normal login page (French, matching `default_locale=fr`) |
| `docker logs openmrs-app` after restart | clean, no new errors |

**NOT fixed — could not be captured this session:** the *original* exception that the
error page was trying to display in the first place. It only reproduced once, in one
browser tab's existing session; after the restart (which invalidates in-memory Tomcat
sessions), the same navigation now succeeds cleanly and the trigger condition could not be
reproduced. Because `MEMORY_APPENDER` was broken, that original exception was never written
to any log — this fix does not recover it retroactively.

**If it happens again:** this fix means the error page will now render fully instead of
crashing, so the real exception **will be visible directly in the browser** — capture that
page immediately (screenshot or "View Source") before navigating away or logging out,
since the trigger looked session-specific and may not survive a refresh.

**Still open:** `MEMORY_APPENDER` should be fixed properly (its Log4j config) so future
errors are captured in logs regardless of whether anyone catches the browser page in time.
Not attempted this session — out of scope until the next occurrence gives something
concrete to chase, per this project's "read-only investigation before any write" rule.

**ROLLBACK:**
```bash
docker exec openmrs-app sh -c 'cp /usr/local/tomcat/.OpenMRS/.openmrs-lib-cache/reportingcompatibility/themes/green.properties.bak-20260907-152840 /usr/local/tomcat/.OpenMRS/.openmrs-lib-cache/reportingcompatibility/themes/green.properties'
docker restart openmrs-app
```

---

## 3. Priority 2 — before clinical use

### 3.1 DNS record for `viewer.hospital.lan`

**CLASS:** change on the internal DNS server (not this machine)

**Status:** only an `/etc/hosts` entry on this server. **No workstation can reach the viewer.**

Add an **A record**: `viewer.hospital.lan` → `10.0.211.249`, alongside the existing
`orthanc.hospital.lan` and `openmrs.hospital.lan` records.

**Verify from a workstation** (not the server — the server uses an external resolver and
cannot resolve `hospital.lan`, which is expected and must not be "fixed"):

```bash
nslookup viewer.hospital.lan
```

If a stale negative answer is cached: `ipconfig /flushdns` on Windows.

---

### 3.2 Workstation acceptance test

**CLASS:** READ-ONLY

Use a **private/incognito window**. A browser with cached Orthanc credentials will supply
them silently and make a broken auth chain look healthy.

1. `imaging.ohifBaseUrl` = `https://viewer.hospital.lan`
2. OHIF icon renders on **both** the studies page and the Get studies page
3. Icon opens `https://viewer.hospital.lan/viewer?StudyInstanceUIDs=<uid>`
4. Study loads — no "Data Source Connection Error"
5. **No native password prompt, ever.** If one appears the proxy has been bypassed.
   Typing the password and seeing images is **NOT a pass** — it is proof of failure.
6. DevTools console: no mixed-content warnings, no CORS errors
7. MPR, volume rendering, and a saved segmentation brush stroke all work
   (step 7 requires §2.1 first)

**Server-side evidence** for whatever happened:

```bash
tail -40 /home/server/nginx-proxy-manager/data/logs/proxy-host-3_access.log
```

A healthy load shows `/dicom-web/studies`, then `.../metadata`, then `.../frames/<n>` — all
`200`. If frames return 200 and images still do not appear, the fault is client-side
rendering, not networking.

---

## 4. Priority 3 — hardening

### 4.1 Connection-pool validation (the recurring 500s) — ⚠️ APPLIED 2026-09-07, effectiveness not yet observed

**The originally planned fix would not have worked — caught before applying it.**

The handoff's draft proposed `hibernate.c3p0.preferredTestQuery`,
`idleConnectionTestPeriod`, `testConnectionOnCheckout`. Before writing those, the actual
`hibernate-c3p0-5.4.21.Final.jar` bundled in this deployment was inspected directly
(bytecode string constants, not documentation). **Hibernate's `C3P0ConnectionProvider` in
this version forwards only six `hibernate.c3p0.*` keys** — `min_size`, `max_size`,
`timeout`, `max_statements`, `acquire_increment`, `idle_test_period`. The three originally
planned keys are not among them and would have been silently ignored: written to the
file, loaded, and never reaching c3p0.

**A second correction:** OpenMRS's bundled `hibernate.default.properties` already sets
`hibernate.c3p0.idle_test_period=3000` (50 min) — some validation was already active,
contrary to the original assumption that none existed. The Aug 30 incident happened
despite this. A plausible contributor, not fully confirmed: `hibernate.c3p0.timeout=100`
(also a default, mapping to c3p0's `maxIdleTime`) lets the pool shrink toward
`min_size` (previously **0**) after just 100 seconds of inactivity — connections can be
evicted before the 50-minute test cycle ever inspects them.

**Applied, using only the six confirmed-working keys:**

```
hibernate.c3p0.min_size=2
```

in `openmrs-runtime.properties` (on the named volume, edited via `docker exec`, backed
up first to `backup files/openmrs-runtime.properties.bak-20260907-114427`), followed by
a restart of `openmrs-app` (~2 min downtime, same as the original incident's recovery).

**Verified:** file content correct after edit; OpenMRS came back up cleanly; imaging
module still `1.2.0` and started; `settings.list` and the OHIF/DICOMweb chain both
healthy post-restart; zero stale-connection errors since restart (expected — a fresh
pool has none to shed).

**NOT verified, and can't be from this session:** whether `min_size=2` actually prevents
the multi-day-idle failure. Minutes after restart, 22 connections were still open at
~58s idle — under the 100s `maxIdleTime` threshold, so no shrinkage has occurred yet to
observe either way. The only real test is the same timescale as the original incident:
**does `settings.list` / any admin page still 500 after this server sits idle for
several days?** If it recurs, the stronger fix (a `c3p0.properties` file or
`-Dc3p0.testConnectionOnCheckout=true` JVM flag, both proposed and declined at this
session's decision point) should be revisited.

**ROLLBACK:** `docker cp "backup files/openmrs-runtime.properties.bak-20260907-114427" openmrs-app:/usr/local/tomcat/.OpenMRS/openmrs-runtime.properties && docker restart openmrs-app`

---

### 4.2 Make NPM's network attachment durable — ✅ DONE 2026-09-07

**Applied.** `~/nginx-proxy-manager/docker-compose.yml` now declares both networks:

```yaml
services:
  app:
    networks:
      - default
      - openmrs
networks:
  default:
  openmrs:
    external: true
    name: openmrs-orthanc-integration_default
```

`data/database.sqlite` was backed up first, then `docker compose up -d` recreated the
container.

**Verified against the actual failure mode**, not just config syntax — a full recreate,
not merely a reload:

| Check | Result |
| --- | --- |
| NPM lands on both networks with no manual `docker network connect` | ✅ |
| Resolves `ohif-viewer`, `orthanc-cors-proxy`, `openmrs-app`, `orthanc-pacs` | ✅ |
| `openmrs.hospital.lan` | 302 (normal) |
| `orthanc.hospital.lan` | 401 (normal) |
| `viewer.hospital.lan` `/` | 200 |
| `viewer.hospital.lan` `/dicom-web/studies` | 200 `application/dicom+json` |
| All three `proxy_host/*.conf` intact | ✅ |

**ROLLBACK:** `cat "backup files/nginx-proxy-manager-docker-compose.yml.bak-20260907-113125" > ~/nginx-proxy-manager/docker-compose.yml && cd ~/nginx-proxy-manager && docker compose up -d`
(database backup at `backup files/nginx-proxy-manager-database.sqlite.bak-20260907-113134`, not needed — nothing was lost.)

---

### 4.3 Credentials publicly exposed on GitHub — ⚠️ ACCEPTED RISK for development, MANDATORY before production (recorded 2026-09-07)

**This is not a to-do. It is a decision, made explicitly by the maintainer on 2026-09-07,
to accept a known risk during development.** Do not act on this section without a fresh,
explicit decision if the deployment target ever changes from "development" to anything
resembling production or real patient data.

**What was found**, investigated because the handoff had assumed credentials were merely
"inline in compose files" — a routine hygiene item. The actual finding is more serious:

- The project's GitHub remote, `github.com/bouzenaali/openmrs-orthanc-integration`, is
  confirmed **public** (`"private": false` via the GitHub API).
- `origin/main`'s current tip (not just old history) tracks a **committed `.env` file**
  and `orthanc-docker-compose.yml` with inline secrets.
- **Both are live, not stale placeholders.** SHA-256 of the committed `.env` matches the
  local, currently-used `.env` exactly. SHA-256 of the committed
  `ORTHANC__REGISTERED_USERS` line matches what is actually running in production right
  now, exactly. This was confirmed without ever printing either secret value.
- Consequently: **anyone on the internet can currently read the real Orthanc admin
  password and the real `MEDREPORT_RGS_TOKEN`.** The Orthanc password grants full
  DICOMweb access — including deletion — through the entire auth-injection chain this
  project built.
- The local repository was also found to be **7 commits behind `origin/main`** — someone
  pushed independently of this session (an `ohif integration + hospitalCA certificate...`
  commit, among others). Do not assume the local working tree and `origin/main` match;
  they did not.

**Explicit decision recorded here:** for this **development environment**, the maintainer
has decided **not** to rotate these credentials, **not** to make the repository private,
and **not** to rewrite or purge the exposed git history. This is accepted as-is for now.

**Documentation was updated to reflect this decision** — see `README.md` → *Security
notes* and `OHIF-Integration-Architecture.md` → *Known limitations*, both dated
2026-09-07.

---

**Before ANY production deployment, all of the following are mandatory — not optional:**

1. **Rotate every credential that was ever committed**, not just the Orthanc password:
   the Orthanc admin password, the MySQL root/app passwords, the PostgreSQL password, and
   `MEDREPORT_RGS_TOKEN`. Treat every value that has ever appeared in this repository's
   history as permanently compromised, even after this rotation — see point 3.
2. **Move every secret out of tracked files and into `.env`**, and confirm `.env` is
   actually listed in `.gitignore` (it currently is not) before the next commit.
3. **Address the git history, not just the current tip.** Rotating values going forward
   does not erase them from history — anyone who already cloned the repo, or who reads
   its commit history on GitHub, retains the old credentials indefinitely. Either rewrite
   history (`git filter-repo` or BFG, then a coordinated force-push — highly disruptive to
   any other clones, needs explicit authorisation, out of scope for a routine session) or
   accept that those specific old values must never be reused anywhere, ever, even as a
   "fixed" default.
4. **Make the repository private**, or verifiably confirm production secrets are fully
   decoupled from it (i.e., production would use different credentials than any that were
   ever public, making the public history's exposure moot rather than remediated).
5. Only after 1–4: re-run the sequenced credential change below, and re-verify §3.2.

**The three-places-must-change-together trap** (still true whenever rotation happens):

| Place | What |
| --- | --- |
| `orthanc-docker-compose.yml` | `ORTHANC__REGISTERED_USERS` |
| `orthanc-cors-proxy.conf` | `proxy_set_header Authorization "Basic <base64 of user:pass>"` |
| `imaging_OrthancConfiguration` table | `orthancPassword`, via **Administration → Configuring the Orthanc server** |

Changing only the first produces a working-looking system where OHIF 401s and OpenMRS
uploads fail silently until someone checks.

---

## 5. Priority 4 — housekeeping

| Item | Detail |
| --- | --- |
| **`node:alpine` image (178 MB)** | Pulled during this work for a one-off JavaScript syntax check. Nothing uses it: `docker image rm node:alpine`. |
| **Misleading log level** | `ImagingActivator.java:29` logs `log.error("Started Imaging")` on **success**. Change to `log.info`. Cosmetic but actively misleading during triage. |
| **`.before-persistence` clutter** | Three stale files in `backup files/`. Remove once you are confident the persistence migration is settled. |
| **Version control — partly addressed 2026-09-16/17** | Was: nothing committed. Now: the `chuschedules` module, its design doc and the operational docs are committed and pushed on `feature/chuschedules-recurring-schedules`, and `patches/`, `backup files/` and `module-backups/` are tracked as of `d64d3d5`. **Still true:** `main` lags the working tree until that branch merges, and `git checkout <file>` is not a rollback path for anything untracked — check `git ls-files <path>` first, and keep using `backup files/` and `module-backups/`. The half-migrated submodule state is unchanged, so **still no `git submodule` commands**, and no `git` writes without explicit authorisation. |

---

## 6. Decisions needed

1. **Were `OHIF-Integration-Status.md` and `OHIF-HTTPS-HANDOFF.md` deleted intentionally?**
   Both existed on 2026-08-26 and are gone from this directory. An archived copy of the former
   is at `../openmrs-orthanc-integration-backup_25_08_2026/`. If unintentional, restore it.

2. **Should PACS-deleted studies that are assigned to a patient be preserved?**
   1.2.0 prunes them and logs a `WARN` naming the study and patient. One has already been
   removed (patient 9, study `…1245427`). Rationale: the study no longer exists in Orthanc, so
   every link on it 404s. To preserve them instead, make
   `removeStudiesDeletedFromOrthanc` skip rows where `getMrsPatient() != null` — see the
   module README.

---

## 7. Rollback inventory

| Artifact | Location |
| --- | --- |
| Previous module (1.1.3) | `~/imaging-1.1.3-SNAPSHOT.omod.rollback` (integrity-verified) |
| Previous module (1.1.2) | `~/imaging-1.1.2-SNAPSHOT.omod.rollback` |
| Module source before 1.2.0 edits | `module-backups/20260830-144548/` |
| `green` theme properties before §2.4 favicon fix | inside `openmrs_data` volume: `.openmrs-lib-cache/reportingcompatibility/themes/green.properties.bak-20260907-152840` |
| Config and doc backups | `backup files/*.bak-<timestamp>` |
| NPM state | `~/nginx-proxy-manager/data/database.sqlite` |

Bind-mounted files (`ohif-app-config.js`, `orthanc-cors-proxy.conf`) must be restored
**inode-preserving**:

```bash
cat "backup files/<backup>" > <file>
```
